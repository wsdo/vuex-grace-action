## use
#### createAction
```
const actions = {
  // increment({ state, commit }) {
  //   commit('INCREMENT')
  // },
  increment: createAction('INCREMENT'),
}

```

#### createActionStatus
```
const actions = {
  // increment({ state, commit }) {
  //   commit('INCREMENT')
  // },
  increment: createActionStatus('INCREMENT'),
}

```
